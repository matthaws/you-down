<script type="text/javascript">
<% if logged_in? %>
   window.currentUser = <%= render(
     "api/shared/users.json.jbuilder",
     user: current_user
   ).html_safe %>
 <% end %>

window.images =  {
    default_profile: "<%= image_path("profile-pic.png") %>",
    expand_arrow: "<%= image_path("expand.png") %>",
    collapse_arrow: "<%= image_path("collapse.png") %>"
  }

</script>

<main id="root"></main>
